<div class="container-fluid">

  <p><strong>Title:</strong> <%=@project.title%></p>
  <p><strong>Creator:</strong> <%=@project.user.name%></p>
  <p><strong>Categories:</strong> <%=@project.category%></p>
  <p><strong>Skills:</strong> <%=@project.skills%></p>
  <p><strong>Description:</strong> <%=@project.description%></p>

  <% if @current_user.id == @project.user.id  %>
    <% if @people %>
    <div class="row flex-between">
        <% @people.each do |t| %>
            <div class="card">
                <div class="avatar">
                    <%= image_tag t.avatar.url(:thumb), class: "image-responsive" %>
                </div>
                <div class="content">
                    <%= link_to t.name , user_path(t), class: "btn btn-default" %>
                </div>
            </div>
          <% end %>
    </div>
    <% end %>
  <% else %>

  <div class="row flex-between">
    <% if @interests %>
    <% if @interests.include?(@current_user.id.to_s) %>
      <%= link_to "Not Interest" , uninterested_path(), method: :delete, class: "btn btn-danger" %>
      <% end %>
    <% else %>
        <%= link_to "Show Interest" , interested_path(), method: :post, class: "btn btn-default" %>
    <% end %>
  </div>
<% end %>
</div>
